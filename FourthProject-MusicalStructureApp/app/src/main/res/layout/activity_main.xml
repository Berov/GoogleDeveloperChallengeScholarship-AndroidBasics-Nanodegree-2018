<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="@string/jukebox"
        android:textAllCaps="false"
        android:textColor="@color/colorPrimary"
        android:textSize="@dimen/first_screen_jukebox_name_size"
        android:textStyle="italic" />

    <TextView
        android:id="@+id/textView_songs"
        style="@style/BaseStyle"
        android:text="@string/songs" />

    <TextView
        android:id="@+id/textView_artists"
        style="@style/BaseStyle"
        android:text="@string/artists" />

    <TextView
        android:id="@+id/textView_year"
        style="@style/BaseStyle"
        android:text="@string/year" />

    <TextView
        android:id="@+id/textView_genres"
        style="@style/BaseStyle"
        android:text="@string/genres" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="right"
        android:paddingRight="@dimen/u_padding_right">

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:srcCompat="@drawable/udacity" />
    </LinearLayout>
</LinearLayout>
